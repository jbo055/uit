# write a program that will compute the area of a given radius

radius = eval(input('Enter the radius of your area: ')) #let user input a radius
pi = 3.14

area = (radius ** 2) * pi #compute area

print('The area of ', radius, 'is ', area) #result
